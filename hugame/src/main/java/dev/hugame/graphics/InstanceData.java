package dev.hugame.graphics;

import dev.hugame.graphics.material.Material;
import dev.hugame.util.Transform;

//public record InstanceData(Transform transform, Material material) {
public record InstanceData(Transform transform) {
}
